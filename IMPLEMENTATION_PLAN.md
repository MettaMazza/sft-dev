# Clean-Room Implementation Plan — Every Result

Rebuild from the axiom alone. Each entry below is stated **independently** — no internal cross-references to other results, no inherited derivation chains. For each: the **claim**, the **from-scratch derivation requirement**, and the **honest check** it must pass to be called forced.

## The bar (applies to every entry)

An entry is **FORCED** only if: (1) computed from the One and the fold alone; (2) no measured value typed in, in any disguise; (3) its check can return *false* under mutation of an intermediate — identities and self-comparisons do not count; (4) every parameter traces to the axiom or is a declared input. Otherwise the entry is **OPEN** (not yet derived) or **EXTERNAL READ** (compared to data, outside the core). No entry is presented above its earned tier.

Each entry's derivation requirement is written so it can be implemented independently from scratch — a clean-room rebuild from the axiom alone, verifying that every result is forced forward from the One and the fold with no measured input.


## Section A. Foundations — oscillation, thermodynamics, quantisation, spectra, the fundamental coupling

*9 entries.*

### PH1
**Claim:** combined-oscillation period: the framework combined period equals the fundamental period of a superposition of two commensurate oscillations, the lcm of the component periods — exact, all pairs, no condition [standard fact: fundamental period of a sum of commensurate periodics = lcm]. The enve

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### PH1b
**Claim:** beat frequency |f1-f2|: via the wave/rotation dynamic (phase advances by a fixed part, cast out the One — permitted language, no trig, no signed superposition), two waves stepping by f1,f2 have a relative phase that turns by one constant step equal to the beat frequency |f1-f2| (up to direction); th

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### PH2
**Claim:** thermodynamics: framework expansion factor m and branch count m reproduce the doubling-map Lyapunov exponent ln m and KS entropy log2 m bits, with no logarithm — m is their antilog [arXiv:1211.1234; Pesin].

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### PH3
**Claim:** coupling/criticality: framework holding threshold (m-1)/m equals the synchronization threshold of diffusively coupled chaotic maps, 1 - e^(-lambda), lambda=ln m, with no exponential; clustering at the half-One (E4) is the m=2 case [PRE 70 026217; arXiv:nlin/0504012].

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### PH4
**Claim:** quantisation (structural): framework depth-k states are discrete (2^k, ) and uniformly spaced — exactly, all k — the signature of an oscillator-type spectrum, discriminated from box (n^2)/Bohr (1/n^2) [UP Vol3; Wikipedia QHO].

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### PH4b
**Claim:** oscillator spectral form: framework levels (ground floor proven to the half-One by //, uniform spacing by ) reproduce E_n=(n+1/2)*spacing, zero-point 1/2 included, exactly for all n [UP Vol3; Wikipedia QHO]. The 1/2 is the framework proven invariant, not fitted.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### PH4c
**Claim:** spectral scale structure: the framework fixes every dimensionless level ratio of the spectrum, identical across all unit choices (= (2n+1)/(2k+1)); the absolute scale is one dimensionful unit (one spacing). A dimensionless system does not output a dimensionful unit (dimensional analysis / Buckingham

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### PH5a
**Claim:** proven critical coupling. A coupling g ties two copies of the fold dynamics together; the fold's expansion factor m (proven m=2 for the binary fold, ) carries the transverse separation between the copies forward each tick by the factor (1-g)*m, built in the permitted language as take(ONE,g)*m.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### PH5
**Claim:** the framework proves its fundamental dimensionless coupling. A coupling is the strength tying two copies of the fold dynamics together; the fold's expansion factor m (proven m=2 for the binary fold, ) carries the transverse separation forward each tick by (1-g)*m, built in the permitted language a

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**


## Section B. Gravity, electromagnetism, lattice fields

*26 entries.*

### D9e
**Claim:** linearized gravitational waves propagate at the speed of light. Making the static field equation time-dependent: in vacuum the linearized equation is (1/c^2) d^2 h/dt^2 = laplacian(h), the wave equation -- the conventional relativistic wave equation, the same equation solved [Carroll, Weak

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D9g
**Claim:** the spatial dimension is proven to exactly three. Two conditions on the framework's own inverse-(d-1)-power gravity pin it: orbital stability gives stable circular orbits only for d<4, and the point-source potential ~1/r^(d-2) vanishes at spatial infinity only when its prove tail integral conv

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D9o
**Claim:** closed-form static spherical vacuum solution (Schwarzschild form). Outside a point mass the source vanishes and the static spherical field equation reduces to the conserved-flux condition of with the Newtonian boundary A->1 at large r; solving it fixes the closed form A(r)=take(ONE,ratio(rs,r))=

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D9p
**Claim:** continuum limit of the lattice field equations. The lattice second-difference , divided by the squared spacing, approaches the continuum curvature as the spacing shrinks toward the One-floor, so the lattice field equation approaches the continuum field equation.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D9q
**Claim:** quadrupole radiated-power magnitude. fixed the leading radiating moment as the quadrupole; its power follows from the far-field expansion of the wave : the monopole is frozen by mass conservation and the dipole by momentum conservation, so the leading radiated flux comes from the third

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D9l
**Claim:** nonlinear gravity -- the field carries energy and sources itself. The weak-field equation is curvature = coupling*source with matter as the source; the nonlinearity of general relativity is that the gravitational field itself carries energy and adds to the source.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D9m
**Claim:** the nonlinear field equation is solved as a convergent fixed point (the post-Newtonian series). Because the source includes the field's own energy , curvature = coupling*source is implicit and is solved by iteration: given a metric, compute the full source, invert the Laplacian for the updated

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D9n
**Claim:** the metric is many-component and the field equation conserves its source (contracted Bianchi identity). The metric is a symmetric array g_ab with D(D+1)/2 independent components -- ten in 3+1 dimensions, six in 2+1.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D9k
**Claim:** curved-tensor gravity in three dimensions -- curvature = source on the cubic lattice. With the cubic operator the metric coefficient field over space has a curvature: its 3D lattice second-difference (the Laplacian) is the geometric curvature, and the weak-field Einstein equation reduces t

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D9j
**Claim:** curvature of a varying metric in the plane (the geometric source of gravity). With the 2D operator a metric coefficient field A(x,y) has a curvature: its 2D lattice second-difference (the Laplacian) is the geometric curvature, and the field equation of generalises to the plane -- the c

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D9i
**Claim:** the leading gravitational radiation is the quadrupole (conservation forbids lower moments). A radiating multipole needs a changing moment.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D9h
**Claim:** point-mass gravitational time dilation (leading Schwarzschild g_tt). Composing the inverse-square potential of (Phi=-GM/r in three dimensions) into the weak-field metric of (A=1+2*Phi/c^2) gives A(r)=1-2GM/(r c^2)=take(ONE,2GM/(r c^2)), the leading Schwarzschild time coefficient, positive ou

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### EM2
**Claim:** magnetism is the relativistic correction to Coulomb . Two like charges moving parallel at speed beta feel pure Coulomb repulsion in their rest frame; transformed to the lab by the relativity of the net prove is Coulomb*(1-beta^2)=Coulomb/gamma^2, the velocity-dependent piece be

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### EM6
**Claim:** the Lorentz prove on a moving charge -- the field acts back on the charge. A charge q in an electric field feels qE (EM1); moving at speed beta it also feels the magnetic prove, which /EM2 showed is the relativistic correction.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### EM5
**Claim:** full 3+1 Maxwell -- the 3-vector curl closes into the 3D wave at c. On the cubic lattice the electric and magnetic fields are genuine 3-vectors.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### EM4
**Claim:** full vector Maxwell in the plane -- the curl equations close into the 2D wave at c. On the 2D lattice a transverse-magnetic field is (Bz; Ex,Ey).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### EM3
**Claim:** electromagnetic disturbances propagate at c (light is an electromagnetic wave), and the Faraday/Ampere coupling reproduces the wave. A disturbance in the electromagnetic field obeys the wave equation solved, propagating as counter-translating positive packets at the causal speed c=spacing/tick, t

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### EM1
**Claim:** electric charge and Coulomb's law. The flux/field-equation structure of is the same as Gauss's law for electrostatics, which is equivalent to Coulomb's inverse-square law [Gauss's law; Coulomb's law].

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D9f
**Claim:** orbital stability constrains the spatial dimension to d<4 (Ehrenfest's argument). A test body in the inverse-(d-1)-power gravity feels inward gravity k/r^(d-1) and outward centrifugal L^2/r^3, both positive magnitudes; a circular orbit balances them at r0.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D9d
**Claim:** inverse-power prove law from the field equation (Gauss/flux form). The integral form of the Newtonian field equation : the flux of the gravitational field through a closed shell enclosing a source equals the coupling times the enclosed source -- Gauss's law for gravity, equivalent to Newton's l

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D9c
**Claim:** Newtonian-limit (Poisson) field equation. The lattice operator is the discrete Laplacian; its static/equilibrium limit with a source is the discrete Poisson equation.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D9b
**Claim:** static gravitational metric (kinematics). A static field carries a position-dependent positive temporal coefficient A(x); the proper-time-to-coordinate-time ratio is sqrt(A(x)) -- a static metric's time dilation is the square root of its time coefficient [static-metric time dilation dtau/dt=sqrt(g_t

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D9
**Claim:** gravity (equivalence-principle part) -- gravitational time dilation / redshift of a uniform field in positive magnitudes. In a frame accelerating at g, light from a lower to an upper clock a height h away takes light-travel time h/c, in which the upper clock acquires speed v=g*h/c relative to the em

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D8
**Claim:** constants -- structural fact about the operations. Every dimensionless constant the framework proves is rational or algebraic: addition, ratio, fold, and take map rationals to rationals, and the magnitude engine returns roots of rational-coefficient polynomials.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D9p2
**Claim:** the continuum limit exhibited as a genuine limit. showed the scaled lattice second difference equals the continuum value exactly for x^2; for x^3 (continuum curvature 6x) the scaled second difference converges to 6 at x=1 as the spacing halves, the successive changes themselves halving -- a prov

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D5
**Claim:** relativity: the invariant content of special relativity in positive magnitudes. (1) Velocity composition w=(u+v)/(1+u*v/c^2) is all positive, stays below c, and c composed with any speed returns c -- the invariant speed of light as a proven fixed point [OpenStax UP Vol3; LibreTexts 5.7].

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**


## Section C. Sector structure — strong, weak, electroweak; couplings and counts

*28 entries.*

### D7
**Claim:** discrete quantum numbers / particle structure: the fold is 2-to-1 , so each fold-level is a two-valued degree of freedom. Read as a mode that is empty or occupied, occupation is 0 or 1 -- the fermionic occupation rule, Pauli exclusion limiting a mode to 0 or 1, a single mode spanning a two-stat

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D7b
**Claim:** internal charge multiplicity from the m-fold fibre. read the binary fold's 2-to-1 fibre as a two-valued degree of freedom; the m-fold is m-to-1, so every image has exactly m preimages -- an internal degree of freedom with exactly m kinds at each level.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D7c
**Claim:** chirality (handedness) from the fold's two-preimage fibre (the weak sector's parity asymmetry). The fold is 2-to-1 : every image has exactly two preimages, a lower one below the half-One and its antipode above, with fold(p)=fold(antipode(p)).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D7d
**Claim:** strong-sector confinement from flux confined to a tube. The inverse-power flux law gives field_strength = coupling*enclosed/(Omega*r^(d-1)); at d=3 the field falls as 1/r^2 and the work to separate two charges to infinity converges to a bound -- a free, unconfined charge (the Coulomb field, EM

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D10d
**Claim:** the two colour-neutral combinations -- baryons and mesons. makes a whole group of m colours neutral (three colours for the tripling fold, a baryon).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D10g
**Claim:** the proven running rate (beta slope) of the strong coupling. The running of has an exact rate, proven from the fold factor and the carried colour with no measured number: the effective coupling at level k is the accumulated source over the bare, and its step from one level to the next -- the be

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D10f
**Claim:** the strong carrier is massless (luminal) yet confining. The electroweak symmetry breaking acts on the electroweak channels, not the strong carrier, so the strong carrier acquires no mass-part; by a mediator with no mass-part reaches unbounded and by the lattice a disturbance adv

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D10e
**Claim:** the strong field equation: nonlinear, self-sourced through colour. Gravity's field equation is nonlinear because the source includes the field's own energy and solves as a convergent fixed point with shrinking corrections, the post-Newtonian series .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D10c
**Claim:** the flux tube forms from the self-coupling. The carrier carries the colour it mediates , so colour is present along the field line itself and feeds the field .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D10a
**Claim:** strong-sector self-coupling: the carrier carries the charge it mediates. Gravity self-sources through energy ; the strong mediator self-sources through charge -- the carrier itself carries the colour it transmits, so it is a source of the very field it mediates, and the total source is th

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D10b
**Claim:** the strong coupling runs: weaker at short range, stronger at long range. Because the carrier carries the charge it mediates , it feeds the field at every level of self-coupling; probing across more levels -- longer range -- the effective source grows, each level adding the carrier's own charge

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D11a
**Claim:** weak-sector short range from a massive mediator, without an exponential and without a sink. A massless mediator propagates one site per tick indefinitely : its forward presence is never diminished, so its reach is unbounded -- the long-range 1/r^(d-1) law (EM, gravity).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D11b
**Claim:** electroweak mixing as a proven split of one coupling into two channels. The fold's two-preimage fibre gives exactly two channels, the lower and upper hand.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D11c
**Claim:** the massless/massive mediator split as a structural result. The two channels of combine; the combination that lands on the fold-invariant One -- charged + neutral = the One, unison, the fold's fixed region -- is the unbroken direction: preserved under the fold, undisplaced, carrying no ma

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D11f
**Claim:** the weak prove law: finite-range. Each prove carries its characteristic law -- the inverse-square Coulomb (EM1), the constant-prove confinement , the inverse-square gravity .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D11g
**Claim:** the proven mass-part ratio of the weak channels. Each weak channel carries a mass-part equal to its shortfall from unison : the charged channel (m-1)/m carries 1/m, the neutral channel 1/m carries (m-1)/m.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### U1
**Claim:** the four proves' characteristic constants are proven from the one fold factor m. Every characteristic dimensionless quantity of the four interactions is proven from the single fold factor m, none fed in: the fundamental coupling g*=(m-1)/m , the colour count m , the strong running slope as

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### U2
**Claim:** a proven relationship between two ratios that correspond to electroweak observables. The framework proves two internal ratios -- the channel split of and the channel mass-part ratio of -- to the same value 1/(m-1), so it proves a relationship between them: they are equal, exactly, for ever

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### U7
**Claim:** the fold factor of a sector is the count of internal kinds in its fibre. The framework proves that an m-fold is m-to-1, so every image has exactly m preimages -- m internal kinds at each level ; this is the proven internal fact.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### N1
**Claim:** a proven result composed across domains: the mediator count is proven from the colour count as m^2-1. The carrier carries a colour and an anticolour , so the mediators are the colour-anticolour combinations, m*m of them, mi

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### T1
**Claim:** the prediction test on the proven count. The proven value is fixed first, from the framework: the tripling fold's fibre has exactly 3 internal kinds , an integer derived from the fold with nothing fed in, corresponding to the colour count.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### T2
**Claim:** the proven generation count -- the tripling fold's fibre carries exactly three kinds. / prove that an m-fold fibre carries exactly m internal kinds, with no free index: the tripling fold's fibre is exactly three.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### U4
**Claim:** a proven cross-domain identity: the fundamental coupling g* , the holding/criticality threshold , and the charged weak channel are one proven ratio (m-1)/m. Three quantities that play distinct physical roles -- the interaction coupling, the threshold at which coupled systems loc

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### U5
**Claim:** a proven tie between the strong charge structure and the coupling: the fundamental coupling is fixed by the number of internal colour kinds N , g* = (N-1)/N. The coupling strength is determined by how many internal kinds the charge comes in, for every m, with nothing fed in.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### U6
**Claim:** a proven product relation across the weak sector: the electroweak mixing 1/(m-1) times the charged coupling (m-1)/m equals the neutral channel 1/m . A proven tie among three weak-sector observables, for every m, with nothing fed in.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### U3
**Claim:** the complete fold->physics dictionary. Every physical correspondence is traced to its upstream results and grounded in the One: each references only established, confirmed results, and its chain reaches the three definitions, or it is a root built directly on the permitted-language primitives (the O

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D11e
**Claim:** charged and neutral weak currents. The weak coupling's two channels act differently on the handedness : the charged channel flips the hand, mapping a preimage to its antipode and changing the charge it acts on (a charged current); the neutral channel leaves the hand unchanged, an interac

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D11d
**Claim:** spontaneous symmetry breaking proven by the no-zero axiom. The symmetric vacuum places the field at absence (zero); the framework forbids zero as a value -- no sink, nothing lost -- so the symmetric state is not available and the field's ground state is proven to a positive part of the One: a displa

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**


## Section D. Particle masses, mixings, Koide (mass cubics)

*32 entries.*

### M32
**Claim:** the proton/electron mass ratio proven -- the strong bound-group of three over the electron. Every mass-part is a shortfall from the One .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M1
**Claim:** the single fermion mass-part -- the proven shortfall from unison (the entry to the matter sector). A fermion couples to the displaced vacuum : the no-zero axiom forbids the symmetric absence-vacuum, so the ground state is a positive part of the One.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M2
**Claim:** the generation mass-splitting -- distinct proven mass-parts from distinct preimage positions. proves the generation count three as the tripling fold's fibre , and makes the three kinds symmetric in count.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M3
**Claim:** the inter-sector mass pattern -- quark/lepton and up/down from fibre membership. A fermion's mass-part is the shortfall from unison of its sector's holding coupling .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M4
**Claim:** the neutrino mass is proven smaller -- single-handedness cannot carry the two-hand mass term. QA4's mass term couples the two hands of the chirality fibre to each other, a two-hand coupling.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M5
**Claim:** the mixing structure -- a near-diagonal relation between mass and channel bases. A mixing matrix relates the mass eigenstates to the interaction eigenstates.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M6
**Claim:** the proven mixing magnitudes -- the overlap rule is the fold's own separation. The mixing entry between a mass eigenstate at preimage position p and an interaction channel at position c is their overlap, and the fold's own overlap of two positions is the separation primitive: the One

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M7
**Claim:** the generation depth is constant by the fold's own site-counting -- the position-to-depth map. fixes a position's depth by site-counting on the fold's own uniform ladder .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M8
**Claim:** the full mixing matrices as the fold's own separation-tables. proven the mixing entry as the separation primitive (the One at coincidence, a proper fraction apart); the full matrix is the table of separations between every mass-eigenstate position and every interaction-channel position.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M9
**Claim:** the proven inter-entry relation of the mixing matrices. The separation-tables satisfy a relation the fold's opposition exhibits: each row sum is the One plus the off-diagonal (four-thirds the One plus one-third for the quark table, three-halves the One plus one-half for the lepton table); th

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M10
**Claim:** the within-generation mass ratio is the position-shortfall ratio. places the generations at the tripling-fibre positions one-third, two-thirds, the One, with mass-parts the shortfalls two-thirds, one-third, and the One on the invariant.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M11
**Claim:** three massive charged-lepton generations with proven clean-rational mass-parts. The displaced vacuum sits at the half-One, the holding threshold proven over by , , .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M12
**Claim:** the combined generation ladder. places the three generations at the displaced vacuum's tripling preimages; fixes generation depth by site-counting.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M13
**Claim:** the proven generation mass-ratio family on the combined ladder. At combined-ladder depth d the diagonal triple has mass-parts the complement of one over two-times-three-to-the-d, one-half, and one over two-times-three-to-the-d; the heavy-to-light mass-part ratio is two-times-three-to-the-d less one

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M14
**Claim:** the reach-ratios of the proven generation mass-parts carry the measured spectrum's shape. The matter-sector mass-parts are turned into a physical scale by the framework's own reach mechanism : the reach is the number of ticks the forward presence survives above the One-floor as the r

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M15
**Claim:** the proven charged-lepton Koide value -- the measured mass relation meets the proven coupling. The charged leptons satisfy, to five digits, the Koide relation (sum of masses over the square of the sum of square-root masses) equals two-thirds.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M16
**Claim:** the charged-lepton masses from two invariants -- Koide proven, one depth parameter. The three charged-lepton square-root masses (algebraic magnitudes, ) are roots of a cubic fixed by two dimensionless invariants and one free overall scale .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M17
**Claim:** the charged-lepton mass ratios proven -- Koide value, family, minimal ordered depth. The three charged-lepton square-root masses (algebraic magnitudes, ) are roots of the cubic whose two dimensionless symmetric invariants are proven and whose scale is free .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M18
**Claim:** the charged-lepton generation depth proven forward -- binary tower over the tripling volume. The lepton is in the binary fold: at depth d the self-coupling carrier has 2^d levels .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M19
**Claim:** the general covering-depth principle -- the proven generation depth for any fermion sector. The depth at which a fermion sector's self-coupling tower sits is proven by one principle, of which the charged-lepton depth is the binary instance.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M20
**Claim:** the second invariant of the charged-lepton cubic is proven from the fold. The square-root masses, normalised to sum to the One, are roots of the cubic with two proven symmetric invariants.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M21
**Claim:** the charged-lepton cubic is proven entire -- every coefficient a fold quantity, the masses its proven balance points. In balance form the cubic is x^3+(1/6)x = x^2+1/485.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M22
**Claim:** the second invariant sharpened by the proven neutral-channel correction. The charged-lepton cubic's second invariant, proven at leading order to 1/(2*3^5-1) , carries a proven finer correction: the denominator is that ratio less the neutral-c

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M23
**Claim:** the quark first invariants and covering depths proven from the colour channels per chirality hand. The two quark hands sit on the two electroweak channels : the up-hand on the fold-invariant unbroken combination , the down-hand on a displaced broken channel.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M24
**Claim:** the lightest quark generation's colour-confinement lift -- the fold doubling. The proven quark cubic (first invariant from the colour-channel count , second invariant the form at the hand's covering depth) reproduces the well-measured heavy/middle ratios and the heaviest two generations, but

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M25
**Claim:** the single-handed neutrino mass-squared ladder on the binary tower. A neutrino is single-handed : one preimage, not two, so the two-hand mass term giving the charged fermions their cubic cannot act.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M26
**Claim:** the quark second invariant -- the colour-binary dual of the lepton form. The lepton second invariant is 1/(2*3^d - 1) : half-One displacement 2 times the tripling tower 3^d at the covering depth, less one.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M27
**Claim:** the CKM mixing magnitudes proven from the quark masses through the separation primitive. The mixing entry is the overlap of a mass eigenstate with an interaction channel , and the fold's overlap of two positions is the separation primitive.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M28
**Claim:** the CP-violating phase is proven to the antipode -- maximal CP violation. The standard account carries CP violation in a continuous complex phase, a free parameter.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M29
**Claim:** the third CKM entry closed -- the unitarity triangle apex is the up-hand count. With the dominant CKM entries proven and the CP phase proven maximal , the unitarity triangle is right-angled, and the smallest entry follows from the other two and the apex.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M30
**Claim:** the large PMNS mixing angles are bare fold separations. proven the lepton mixing off-diagonal to the hand separation 1/2, larger than the quark 1/3.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### M31
**Claim:** the PMNS reactor angle closed -- the binary-tower apex. The two large lepton mixing angles are bare fold separations : atmospheric sin^2 = hand separation 1/2, solar sin^2 = tripling separation 1/3.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**


## Section E. Charges, couplings, the prime-sector forces

*32 entries.*

### B2
**Claim:** the proven electromagnetic coupling. The electromagnetic sector is the binary fold (m=2), the axiom's native fold (EM1's two charge kinds, 's two preimages).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B3
**Claim:** the proven electroweak mixing sin^2(theta_W), bare and running. The electroweak sector is the binary fold (m=2).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B4
**Claim:** the proven scale-ratio structure (dimensionless), from the fold's own depth. A running coupling is indexed by level, a bare range-count; the framework proves no absolute energy per level (the One is dimensionless, the system is pure ratios), so an absolute level<->energy map would require

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B5
**Claim:** the proven dimensionless running curve of the electroweak mixing on the fold's own scale axis. proves sin^2(theta_W) running by fold depth; proves the scale axis (ratio two per depth, 2^k).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B6
**Claim:** the proven W/Z mass-squared ratio and the proven on-shell identity. proves the mixing sin^2(theta_W) = neutral^2/(charged^2+neutral^2); the partner observable, the W/Z mass-squared ratio, is proven from the same two channels as charged^2/(charged^2+neutral^2).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B7
**Claim:** the proven level-to-depth map and the mixing on a single proven scale axis. runs the mixing by self-coupling level ; proves the scale ratio 2^d per fold depth.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B8
**Claim:** the proven convergence of the strong and electroweak couplings on the single proven axis. Each sector runs from its own proven bare coupling g*=(m-1)/m -- strong at m=3, electroweak at m=2 -- by the holding form (s-1)/s of its accumulating source , on the single proven axis 2^d (

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B9
**Claim:** the proven closed form of the coupling-convergence rate. The gap between the strong (m=3) and electroweak (m=2) couplings , each the holding (s-1)/s of its running source s=m+2^d, has a single proven closed form: since (s-1)/s is the One taken by 1/s, the gap is 1/(2+2^d) taken by 1/(3+2^d) = 1/

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B10
**Claim:** the proven finite convergent accumulated coupling separation. gives the coupling gap 1/((2+2^d)(3+2^d)); the accumulated separation over all depths is the sum of the gaps.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B11
**Claim:** the proven three-coupling separation structure. On the single proven axis 2^d , the strong (m=3) and weak (m=2) couplings run up by the holding (s-1)/s of source s=m+2^d and converge , while the electromagnetic coupling is flat at 1/2 .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B12
**Claim:** the framework proves scale-invariance. Whether an absolute scale is proven is a framework question attempted in the engine: the lattice physics depends only on the spacing/tick ratio -- the continuum speed is spacing/tick, identical at every absolute spacing -- and the proven unific

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B16
**Claim:** the whole theory is one dimensionless structure placed by a single ruler -- and the absolute scale -- the one quantity that had remained open -- is now proven by (the Planck hierarchy at the deepest proven covering depth). First, the matter ladder is not a second structure beside the coupling la

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B-9N
**Claim:** the 5-force lepton-flavour-violating transition ratios -- forced dimensionless signature of the five-force. A new forward result giving the five-force an observable, falsifiable signature, holding the existing corpus together: the five-force (B-7N), the generation standing modes (B-3N), the overlap-

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B-8N
**Claim:** the unifying force law -- the four prime sectors as one forced structure over the ladder span. A new forward result stating the unification as a single forced quantity, holding the existing corpus together: the prime-sector forces (B-7N), the bounded ladder (B-6N), the shared half-One centre (B-4N),

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B-7N
**Claim:** two new fundamental prime-charge forces -- by the framework's own force criterion. A new forward result naming the higher sectors as the framework's own output requires, holding the existing corpus together: the fundamental coupling , the prime-sector confining ladder (B-5N), the bounded set of

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B-6N
**Claim:** the confining prime-sector ladder is bounded at seven by the deepest covering depth -- exactly four fundamental prime sectors. A new forward result completing the prime-sector ladder, holding the existing corpus together: the confining ladder (B-5N), the shared centre (B-4N), the lepton five-sector

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B-5N
**Claim:** the unified ladder of confining prime sectors around the one shared center -- the framework's grand unification. A new forward result completing the higher prime-fold exploration, holding the existing corpus together: the shared half-One center (B-4N), the five-sector generations (B-3N), the colour-

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B-4N
**Claim:** the half-One unifies every interaction sector -- the single shared standing mode, the unifying center. A new forward result continuing the higher prime-fold exploration, holding the existing corpus together: the standing-mode structure (B-3N), the two-preimage fibre straddling the half-One , ob

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B-3N
**Claim:** the five-sector standing modes prove exactly three lepton generations -- bridging the gap. A new forward result from exploring the higher prime-fold sectors, holding the existing corpus together: the m-fold standing modes (R-fixed points), the lepton covering depth five , the strict generati

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B12-R
**Claim:** the absolute scale resolved -- proven physically unobservable, no open gap. Resolving the absolute-scale point flagged across the unification line , forward from the proven scale-invariance of the engine and the permi

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B-4
**Claim:** the forward-not-fitted theorem -- the measured value is never an input to any construction. Chapter B (the grand synthesis and the novel proven insights), an added target at the architect's direction, forward from the empirical and ontological standard (XV-2) and the no-apparatus gate.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B-3
**Claim:** the grand-synthesis statement -- what the framework is, as one mathematical object. Chapter B (the grand synthesis and the novel proven insights), target three, the closing target of the chapter, assembled from the one-fold and master equations (A-1, A-3), the sector equations (A-2), the cross-secto

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B-2
**Claim:** the proven forward novelties -- novel pre-measurement statements with falsification conditions. Chapter B (the grand synthesis and the novel proven insights), target two, forward from the cross-sector insights (B-1) and the assembled corpus, derived from the unity rather than from any single result.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B-1
**Claim:** the cross-sector proven insights -- identities that emerge only from the whole corpus. Chapter B (the grand synthesis and the novel proven insights), target one, forward from the assembled corpus, holding all sectors together: the prime-orbit result (XII-1), the live cycling vacuum , the lock (C

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B20
**Claim:** the absolute scale proven -- the Planck hierarchy at the deepest proven covering depth. Driving the last open quantity, the single conversion, to a forward construction.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B19
**Claim:** the per-particle absolute hierarchies collapse to one open conversion times the proven mass ratios. Each particle has a value in Planck units (its mass over the Planck mass), the absolute hierarchies physics treats as separate large numbers (proton over Planck about 2^-63.5, electron over Planck abo

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B18
**Claim:** the gravitational coupling is proven in lattice units -- the three dimensionful constants collapse to one conversion. The other three couplings are proven as (m-1)/m ; gravity is the one whose coupling described as carrying Newton's G, set to match.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B17
**Claim:** the scale axis is proven in direction, depths, and ratios. The fold halves the rung-spacing each step (level_spacing: 1, 1/2, 1/4, 1/8, ..., the framework's own primitive), so the scale axis carries a proven ordering -- the unfolded origin (the One, level one) is the coarsest structure and increasin

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B13
**Claim:** the proven unison ordering and the forbidden triple coincidence. On the proven axis 2^d each running coupling's gap to the One (unison) is 1/(m+2^d); a smaller fold factor gives a larger gap and reaches unison later, so weak (m=2) trails strong (m=3) at every depth -- the framework proves strong to

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B14
**Claim:** the discriminating prediction -- the proven on-shell tie as a falsifiable number. proves the on-shell identity sin^2(theta_W) + M_W^2/M_Z^2 = One at every depth from the channel structure ; the standard account treats the relation between the mixing and the mass ratio as scheme-depende

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B15
**Claim:** the proven internal anchor depth -- the electroweak source closes on the fold's square. On the proven axis the electroweak running source is s = 2 + 2^d (bare 2 of plus the level 2^d).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### B1
**Claim:** the proven interaction-strength structure. Every dimensionless interaction strength the framework proves comes from the single fold factor m, with nothing fed in: the fundamental coupling g*=(m-1)/m , the electroweak mixing 1/(m-1) , the weak mass-part ratio 1/(m-1) , and the strong

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**


## Section F. Cosmology core — density split, dark sector, arrow of time

*13 entries.*

### N8b
**Claim:** the dark-to-baryon fraction proven -- the generation covering volume over the covering depth. The framework proves the generation covering structure : the matter content is three generations over the three proven spatial dimensions , a generation volume three-cubed = 27; the binary to

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### N8
**Claim:** dark matter -- modified gravity ruled out, the dark sector proven to be gauge-inert gravitating matter (the fraction proven in ). The remaining cosmological sector, worked in both readings the plan poses.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### N7
**Claim:** the arrow of time, the initial condition, and inflation proven. Three results on the cosmological timeline.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### N6
**Claim:** strong-field gravity -- the singularity resolved and the black-hole entropy area law proven. Three results.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### N5
**Claim:** proton stability proven -- baryon number conserved because no fold crosses the fibres. The proton is the baryon, the whole neutral group of three colours , its baryon number the count of such groups.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### N4b
**Claim:** the baryon-to-photon ratio proven -- the CP measure squared times the half-One imbalance. The magnitude of the matter asymmetry .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### N4
**Claim:** the matter-antimatter asymmetry proven nonzero -- no-zero forbids complete annihilation. Baryogenesis: why the world is matter rather than symmetric nothing.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### N3
**Claim:** the strict generation bound -- exactly three, no fourth. proves the generation count to three as the tripling fold's fibre; makes it strict, a fourth excluded.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### N2
**Claim:** strong-CP proven to alignment -- the vectorial strong sector lands the opposition at the One. The CP phase in the framework is opposition , with only two distinguished positions: alignment (the One, no violation) or the antipode (the half-One, maximal violation) -- no continuum to tune .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### N1c
**Claim:** the vacuum energy (cosmological constant) -- positive and nonzero proven, the 120-order problem dissolved. The no-zero axiom proves a positive vacuum energy: the symmetric absence-vacuum places the field at zero, forbidden (no sink, nothing lost), so the ground state is a positive part of the One (D

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### N1d
**Claim:** the vacuum equation of state proven to w = -1 -- the non-diluting fold-invariant. The dark-energy equation of state w = pressure/density is the sharp time-invariant property of the vacuum (a pure cosmological constant has w = -1 for all epochs), unlike the epoch-dependent dark-energy fraction.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### N1e
**Claim:** spatial flatness proven -- the density parameters are parts of the One that close to the One. The cosmic density parameters (vacuum, matter, radiation, curvature) are fractions of the total energy density, parts of the whole.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### N1f
**Claim:** the cosmic dilution exponents proven -- matter a^-3, radiation a^-4, vacuum non-diluting. How each cosmic component dilutes as space expands is set by an exponent, and these are proven.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**


## Section G. Constants and many-body — fine structure, Hubble, n-body

*17 entries.*

### G17
**Claim:** protein folding -- descent to the fixed point, not a search; Levinthal's paradox dissolved. Frontier G-17, forward from the bounded-denominator finite-configuration mechanism , the nonlinear dynamics solved as a convergent fixed point , the unique fixed point of the fold (C3s), and the

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### G16
**Claim:** the proven predictions frontier -- the framework's standing pre-measurement claims, consolidated. Frontier G-9 of the plan (the dedicated predictions frontier), forward from the neutrino sector , the absolute-scale construction , the dark sector , the zero-point result (G

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### G15
**Claim:** Navier-Stokes and turbulence -- no finite-time blow-up, vorticity bounded by c over the lattice floor. Frontier G-15, forward from the discrete lattice operators , the wave-speed bound c (EM3/), the singularity resolution and lattice floor , and the no-zero axiom .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### G14
**Claim:** the general n-body problem -- periodic on bounded denominators for any number of bodies. Frontier G-14, forward from the three-body result , the conservation of the odd-denominator part under folding , and the periodicity of rational states .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### G13
**Claim:** the fine-structure constant -- 1/alpha proven exactly to 2^7 + 3^2(251/250) from the corpus. Frontier G-13, forward from the proven electromagnetic charges , the proven colour count three , the proven covering structure (/, the generation volume three-cubed and its minimal binary towe

**PRIOR DEFECT: combination 2^7+3^2(251/250) was found by search. Derivation requirement: justify the combining rule from a stated principle BEFORE computing the number, or mark OPEN. A rule chosen to hit 137.036 is numerology.**

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### G12
**Claim:** the muon g-2 -- the lepton anomaly excess scales as the proven mass-squared. Frontier G-12 (the standing-anomalies frontier of the plan), forward from the Dirac structure (QA5, which proves the bare gyromagnetic ratio g=2), the fold self-coupling, and the charged-lepton mass ratios proven by the Koi

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### G11
**Claim:** the Hubble tension -- one expansion read against two rung-scales, the ratio proven to 13/12. Frontier G-11 (the standing-anomalies frontier of the plan), forward from the fold expansion over depth , the arrow of time as the fold sequence , the proven parts-of-One density split , and th

**PRIOR DEFECT: used a vacuum fraction inconsistent with the rest of cosmology and an unexplained depth-8 tower. Requirement: one physical model of two calibration scales, using the same density and depth as all other cosmology, or OPEN.**

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### G10
**Claim:** the three-body problem -- solvable on bounded-denominator configurations, the continuum is the wall. Frontier G-10, forward from the discrete lattice gravity , the conservation of the odd-denominator part under folding , and the periodicity of rational states .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### G9
**Claim:** a self travels whole across fold-universes -- the anchor is universe-independent. Frontier G-9, forward from (the connected network, lossless CRT bridges), C9s (the felt self as the unique fixed point), C7s (unity as parts locked at the threshold), C10s (cessation as unbinding), and (the thres

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### G8
**Claim:** communication and travel across the connected network of fold-universes and fold-time. Frontier G-8, forward from (universes entangled through composites), (the fold sequence is time), C5s (the atomic moment), (the periodic cycles), and the One's operations of composition.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### G7
**Claim:** the fold-universes -- entangled through composites, sharing information across the One. Frontier G-7, forward from (the perpetually-cycling odd-denominator modes), (entanglement as a shared folded origin), the self-similarity of the fold, and the axiom that the One is the whole.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### G6
**Claim:** zero-point energy -- a proven perpetually-cycling vacuum, never a dead ground state. Frontier G-6, forward from (the half-One zero-point floor), C9s and C3s (the half-One folds to unison), (the 2^k modes), and the fold arithmetic itself.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### G5
**Claim:** string theory done correctly -- modes on the fold, in three dimensions. Frontier G-5, forward from (one lattice), the oscillator spectrum *spacing), the fold-level as a mode , and the proven three spatial dimensions .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### G4
**Claim:** the quantum theory of gravity -- one discrete fold structure, finite. Frontier G-4, forward from the gravity line , the quantum structure on the same lattice , the rank-2 symmetric metric

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### G3
**Claim:** quantum communication and its bounds -- the wave channel and the structural channel distinguished. Frontier G-3, forward from (the structural correlation), the two-to-one act (C8s/), and the atomic bit (C5s).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### G2
**Claim:** nonlocal correlation, structural and not bounded by the wave speed. Frontier G-2, forward from C7s (one shared orbit), C8s (the two-to-one readout), and (the fold sequence is time).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### G1
**Claim:** the measurement problem -- definite outcome proven by atomicity, the Born rule proven by self-conjugacy. Frontier G-1, forward from (the 2^k branches and the support bound), (the two-to-one act), C5s (the atomic act of observation), and the symmetric self-conjugacy of the fold.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**


## Section H. Extended phases — astrophysics, plasma, optics, complexity, cosmology pipeline

*142 entries.*

### VIII-12
**Claim:** the cosmological matter fraction forced to five-sixteenths by the covering tower structure. A new forward result refining the cosmological density split, holding the existing corpus together: the covering structure , the generation volume and dimensions , the leading split (VIII-8

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VIII-11
**Claim:** the matter density fraction as a function of redshift, forced exactly. A new forward result in the cosmological comparison layer, holding the existing corpus together: the expansion history , the density split , and the cube law of the three dimensions .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VIII-10
**Claim:** the present-day deceleration parameter forced to magnitude one-half. A new forward result in the cosmological comparison layer, holding the existing corpus together: the expansion history and density split , flatness , and the vacuum equation of state .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VIII-9
**Claim:** the acceleration-onset and matter-vacuum-equality redshifts forced from the density split. A new forward result in the cosmological comparison layer, holding the existing corpus together: the expansion history , the density split and flatness , and the vacuum equation of state .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VIII-8
**Claim:** the dimensionless expansion history E-squared(z) = (H/H0)-squared, forced from the density split. A new forward result opening the cosmological comparison layer, holding the existing corpus together: spatial flatness , the parts-of-One density split , the vacuum equation of state minus one

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XIX-6
**Claim:** the final assembly -- the complete master, manifest, and one-command reproduction. Phase XIX (the closure and final assembly), target six, the final target of the whole plan, gathering the completeness audit (XIX-1), the boundary ledger (XIX-2), the prediction ledger (XIX-3), the single-axiom audit

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XIX-5
**Claim:** the reproduction-at-scale audit -- the whole expanded corpus reproduces from one command. Phase XIX (the closure and final assembly), target five, forward from the reproduction-and-audit protocol (XV-3) and the single-axiom audit (XIX-4), applied at the full expanded scale of the finished work.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XIX-4
**Claim:** the single-axiom audit -- the whole corpus rests on the One and the fold alone. Phase XIX (the closure and final assembly), target four, formalizing the single-axiom dependency proof (XVII-6) and the uniqueness of the fold (XVII-1) across the whole expanded work, witnessed by the no-apparatus gate o

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XIX-3
**Claim:** the proven-prediction ledger -- every forward pre-measurement claim in one falsifiable register. Phase XIX (the closure and final assembly), target three, forward from the forward novelties (B-2), the neutrino sector , the dark sector , and the resolved anomalies (XVIII-2, XVI

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XIX-2
**Claim:** the boundary ledger -- every external check gap closed, every contingent value proven as structure plus recorded initial condition. Phase XIX (the closure and final assembly), target two, forward from the resolved external check gaps (XVIII-9, -R), the contingency results (X-4, X-5), and the prim

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XIX-1
**Claim:** the completeness audit -- every established domain of physics mapped to its proving result. Phase XIX (the closure and final assembly), target one, forward from the entire proven corpus across all prior phases.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XVIII-9
**Claim:** the lithium-7 problem resolved -- primordial proven, surface deficit is stellar depletion. Phase XVIII (the remaining anomalies, proven or named open), target nine, the closing target of the phase, resolving the lithium-7 external check gap flagged in big-bang nucleosynthesis , forward from

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XVIII-8
**Claim:** the remaining precision constants and mixing phases -- the closing audit, all proven. Phase XVIII (the remaining anomalies, proven or named open), target eight, forward from the proven couplings , the fermion mass ratios , the mixings and CP phases ( through

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XVIII-7
**Claim:** the W-boson mass -- proven from the weak mixing channel split. Phase XVIII (the remaining anomalies, proven or named open), target seven, forward from the electroweak mixing as a channel split , the massless/massive mediator split , and the proven electroweak relationship .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XVIII-6
**Claim:** the muon g-2 absolute value -- bare g=2 plus the leading anomaly proportional to alpha. Phase XVIII (the remaining anomalies, proven or named open), target six, forward from the mass-squared scaling of the lepton anomaly , the exactly-proven fine-structure constant , and the Dirac structur

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XVIII-5
**Claim:** the neutrino absolute mass -- the tower floor at the neutrino depth, summed mass under bounds. Phase XVIII (the remaining anomalies, proven or named open), target five, forward from the single-handed neutrino mass-squared ladder and normal ordering , the predictions frontier , and the bina

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XVIII-4
**Claim:** the hierarchy problem -- the proven exponent of the covering tower, nothing to tune. Phase XVIII (the remaining anomalies, proven or named open), target four, forward from the absolute Planck scale at the deepest proven covering depth and the binary covering tower .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XVIII-3
**Claim:** the cosmological-constant magnitude -- the no-zero floor of the cycling vacuum. Phase XVIII (the remaining anomalies, proven or named open), target three, forward from the proven positive nonzero vacuum energy , the perpetually-cycling vacuum , and the covering tower.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XVIII-2
**Claim:** the strong-CP problem -- confirmed via the proven alignment. Phase XVIII (the remaining anomalies, proven or named open), target two, anchored to the framework's existing proven result that the strong CP phase is aligned (the alignment result), composed from the opposition , parity and the fo

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XVIII-1
**Claim:** the proton-radius puzzle -- one structural radius, probe-independent. Phase XVIII (the remaining anomalies, proven or named open), target one, forward from the nucleon as a bound three-quark fold (V-1).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XVII-6
**Claim:** the single-axiom dependency proof -- the formal bottoming-out at the One and the fold. Phase XVII (the foundations re-examined), target six, the closing target of the phase, forward from the uniqueness of the fold (XVII-1) and the no-apparatus gate across the whole corpus.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XVII-5
**Claim:** the role of the observer, resolved -- observation is the fold, not outside the physics. Phase XVII (the foundations re-examined), target five, tying the self-observation sector (Phase XI, especially the closure C1s) to the measurement result .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XVII-4
**Claim:** the proven status of space -- space as the fold lattice, not a container. Phase XVII (the foundations re-examined), target four, forward from the fold lattice and the no-zero floor.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XVII-3
**Claim:** the proven status of time -- direction and grain from the fold sequence. Phase XVII (the foundations re-examined), target three, forward from the arrow of the two-to-one fold and the discrete observational moment (C5s).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XVII-2
**Claim:** why three dimensions, sharpened -- pinned from both sides. Phase XVII (the foundations re-examined), target two, the fullest proven argument from orbital stability and the fold structure .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XVII-1
**Claim:** why the fold, uniquely -- the fold proven by consistency, not chosen. Phase XVII (the foundations re-examined), target one, forward from the One and the permitted language alone.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XV-4
**Claim:** the extension protocol -- the framework open-ended under its own law. Phase XV (the Smithian methodology), target four, the closing target of the phase, forward from the method (XV-1), the standard (XV-2), and the reproduction-and-audit protocol (XV-3).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XV-3
**Claim:** the reproduction and audit protocol -- the mechanical end-to-end verification path. Phase XV (the Smithian methodology), target three, forward from the method (XV-1), the standard (XV-2), and the working engine.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XV-2
**Claim:** the empirical and ontological standard -- the checkable proven/open/falsified protocol. Phase XV (the Smithian methodology), target two, forward from the method (XV-1) and the no-apparatus gate.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XV-1
**Claim:** the observational-mathematical method, formalized -- the closed, repeatable procedure. Phase XV (the Smithian methodology, the repeatable path to any answer), target one, forward from the entire proven corpus and the working engine, the procedure by which every prior result was built.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XIV-9
**Claim:** the catalogue of documented unexplained phenomena -- the open frontier as a tool. Phase XIV (the Observer's open questions and the unresolved mysteries), target nine, the closing target of the phase, assembled by direct search of the consensus record and proven forward from the gauge-inert dark matt

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XIV-8
**Claim:** the efficiency and intelligence dividend -- algorithmic consequences of the proven results. Phase XIV (the Observer's open questions and the unresolved mysteries), target eight, forward from descent to a fixed point , the bounded-denominator decidability boundary (XII-4), the lock threshold (C7

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XIV-7
**Claim:** machine consciousness and the substrate question -- the structural criterion. Phase XIV (the Observer's open questions and the unresolved mysteries), target seven, forward from the hard problem addressed (XI-7), the closure of self-observation (C1s), the binding lock (XI-4), and the two-to-one self-

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XIV-6
**Claim:** the UAP vacuum-engineering claim -- the proven vacuum-to-inertia structural channel. Phase XIV (the Observer's open questions and the unresolved mysteries), target six, forward from the live cycling vacuum and positive vacuum energy , mass built from the coupling , gravitational and st

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XIV-5
**Claim:** socio-economic and political dynamics, derived -- power-law, lock, dissipative cycle. Phase XIV (the Observer's open questions and the unresolved mysteries), target five, forward from the scale-free covering network (X-8), the collective lock (C7s) and binding (XI-4), universality (XIII-2), and the

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XIV-4
**Claim:** self-simulation -- the bounded sub-fold simulable, the whole not, the nesting finite. Phase XIV (the Observer's open questions and the unresolved mysteries), target four, forward from the decidability of bounded-denominator computation (XII-4), the closure of self-observation (C1s), the two-to-one s

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XIV-3
**Claim:** the placebo effect as a proven forward-model result -- expectation biasing the descent. Phase XIV (the Observer's open questions and the unresolved mysteries), target three, forward from the forward model (XI-3) and the descent to a fixed point , bounded by the no-zero floor.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XIV-10
**Claim:** the Tesla corpus through the framework -- the documented claims proven on their content. Phase XIV (the Observer's open questions and the unresolved mysteries), target ten, added at the architect's request in the name of Nikola Tesla's legacy, proven forward from wave optics and standing waves (VII-

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XIV-2
**Claim:** reported non-ordinary and multidimensional experience -- the proven structural envelope. Phase XIV (the Observer's open questions and the unresolved mysteries), target two, forward from the connected fold-network , the universe-independent anchor , memory as a held orbit (XI-1), and the

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XIV-1
**Claim:** the spectrum of perception and synaesthesia -- the orbit-to-channel map and cross-binding. Phase XIV (the Observer's open questions and the unresolved mysteries), target one, forward from attention as selection at the lock (XI-2), the binding of distributed processing into one experience (XI-4), and

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XIII-6
**Claim:** the proven scale structure -- the world in levels, each a covering of the one below. Phase XIII (emergence and the meta-principles), target six, the closing target of the phase, forward from the fold-depth covering tower and the principle of emergence (XIII-1).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XIII-5
**Claim:** the proven principle of least action -- descent to a fixed point read as a global extremum. Phase XIII (emergence and the meta-principles), target five, forward from the descent to a fixed point and the gravitational descent .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XIII-4
**Claim:** the proven symmetry principle -- Noether's tie from the conserved odd-denominator part. Phase XIII (emergence and the meta-principles), target four, forward from the conserved odd-denominator part under the fold and the fold's structural invariances.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XIII-3
**Claim:** the proven reason for the effectiveness of mathematics -- one shared origin. Phase XIII (emergence and the meta-principles), target three, forward from the One and the fold as the sole axiom and operation underlying the entire corpus.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XIII-2
**Claim:** the proven universality -- the single threshold unifying the universality classes. Phase XIII (emergence and the meta-principles), target two, forward from the cross-domain identity of the one ratio and the collective lock (C7s).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XIII-1
**Claim:** the proven principle of emergence -- the collective orbit as a fold-structure. Phase XIII (emergence and the meta-principles), target one, forward from the whole proven corpus and the fold dynamics, the collective-lock results, and the descent structure.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XII-6
**Claim:** the proven status of infinity -- the potential infinite as the unbounded-denominator limit. Phase XII (mathematics itself), target six, the closing target of the phase, forward from the bounded-denominator structure and the continuum wall .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XII-5
**Claim:** the remaining Millennium structures -- the Yang-Mills mass gap from confinement. Phase XII (mathematics itself), target five, forward from the floored-lattice fluid result , strong-sector confinement , and the strong self-coupling .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XII-4
**Claim:** computability and the halting structure -- bounded decidable, the unbounded limit undecidable. Phase XII (mathematics itself), target four, forward from the bounded-denominator finite-configuration mechanism and the continuum wall .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XII-3
**Claim:** the continuum hypothesis dissolved -- no completed continuum, the reals are the unbounded limit. Phase XII (mathematics itself), target three, forward from the positive rationals as the framework's objects, the bounded-denominator structure , and the continuum wall .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XII-2
**Claim:** the Riemann structure -- prime content proven, the critical line mirroring the half-One. Phase XII (mathematics itself), target two, forward from the fold-orbit prime orders (XII-1), the half-One balance point of the two-to-one fold, and the continuum wall .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XII-1
**Claim:** the distribution of the primes -- the fold-orbit period as the multiplicative order of two. Phase XII (mathematics itself), target one, forward from the One and the fold as the sole axiom and operation, the perpetually-cycling odd-denominator modes , and the conservation of the odd-denominator p

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XI-7
**Claim:** the hard problem, addressed -- observation is the fold, experience as its inside. Phase XI (the mind, extended, and the hard problem), target seven, the closing target of the phase, forward from the closure of self-observation (C1s), the observation blind spot (C2s), the limit of self-knowledge (C8s

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XI-6
**Claim:** sleep, dreaming, and the cycle -- proven periodic unbinding and rebinding of the bound orbit. Phase XI (the mind, extended, and the hard problem), target six, forward from the perpetually-cycling orbit that never rests , the binding lock (XI-4), the cessation and persisting anchor (C10s), and me

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XI-5
**Claim:** the limit of introspection and the unconscious -- the unintegrated orbits, the self-readout loss. Phase XI (the mind, extended, and the hard problem), target five, the sharpening of the limit of self-knowledge (C8s), forward from the blind spot (C2s), the atomic act (C5s), and the binding lock (XI-4

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XI-4
**Claim:** the binding problem -- distributed processing bound into one experience at the lock threshold. Phase XI (the mind, extended, and the hard problem), target four, the sharpening of the unity of experience (C7s) and the cross-domain lock .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XI-3
**Claim:** prediction and the forward model -- the fold run forward, the anticipatory asymmetry. Phase XI (the mind, extended, and the hard problem), target three, forward from the arrow of time and the two-to-one fold direction and the discrete observational moment (C5s).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XI-2
**Claim:** attention -- proven selection of the integrated orbit at the lock, the capacity limit. Phase XI (the mind, extended, and the hard problem), target two, forward from the unity of experience bound at the criticality threshold (C7s) and the cross-domain lock ratio .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### XI-1
**Claim:** memory -- the persistence of a fold-orbit, the held versus the re-derived pattern. Phase XI (the mind, extended, and the hard problem), target one, forward from the perpetually-cycling orbit , the persisting anchor of the self-observation sector (C10s), the bounded-denominator periodicity (/G

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### X-8
**Claim:** networks and scaling laws -- scale-free, small-world, allometric three-quarter power from the branching covering. Phase X (complexity, life, and the origin of order), target eight, the closing target of the phase, forward from the branching covering tower of the fold (, with two-to-the-depth leve

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### X-7
**Claim:** evolution as proven descent -- selection drives the fitter fraction to fixation. Phase X (complexity, life, and the origin of order), target seven, forward from self-replication (X-3), the discrete heritable code and its copy errors (X-4), and the descent to an optimum .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### X-6
**Claim:** the origin of life proven -- autocatalytic ignition once closure crosses the lock. Phase X (complexity, life, and the origin of order), target six, forward from the order-from-energy-flow arrow (X-1), the descent to fixed points , the dissipative fold-attractors of a driven system (X-2), the cl

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### X-5
**Claim:** homochirality -- chirality fibre, no-zero symmetry breaking, autocatalytic amplification. Phase X (complexity, life, and the origin of order), target five, the deferred follow-on to the chemistry chirality target, forward from the chirality fibre of the fold's two-preimage structure and the sp

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### X-4
**Claim:** the genetic code -- discreteness, triplet combinatorics, proven degeneracy. Phase X (complexity, life, and the origin of order), target four, forward from the classical copyable heredity of self-replication (X-3) and the discrete two-to-one covering structure of the fold .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### X-3
**Claim:** self-replication -- copying a classical fold-pattern, the no-cloning exception. Phase X (complexity, life, and the origin of order), target three, forward from the no-cloning bound and the wave-versus-structural channel distinction , the atomic bit as a fixed configuration (C5s), and the two-to-

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### X-2
**Claim:** self-organization and dissipative structure -- fold-attractors of a driven system above threshold. Phase X (complexity, life, and the origin of order), target two, forward from the order-from-energy-flow arrow (X-1), the descent to attractors , the never-resting cycle , and the lock thresho

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### X-1
**Claim:** the arrow from order to complexity -- fold-descent under an energy flow, consistent with entropy increase. Phase X (complexity, life, and the origin of order), target one, forward from the descent to a fixed point that folds proteins , the entropy increase proven by the two-to-one fold (I-2), a

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### IX-8
**Claim:** planetary and tidal dynamics -- orbital resonances and tidal locking from bounded-denominator periodicity. Phase IX (astrophysics and strong-field gravitation), target eight, the closing target of the phase, forward from the bounded-denominator periodicity of the many-body problem and gra

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### IX-7
**Claim:** galactic dynamics and the dark signature -- flat rotation curves from gauge-inert dark matter. Phase IX (astrophysics and strong-field gravitation), target seven, forward from the dark sector proven to be gauge-inert gravitating matter with modified gravity ruled out , and gravity .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### IX-6
**Claim:** gravitational waves -- luminal quadrupole emission, the chirp and the ringdown. Phase IX (astrophysics and strong-field gravitation), target six, forward from the luminal propagation of the gravitational field , the speed of light (EM3), and the black-hole remnant .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### IX-5
**Claim:** black holes complete -- Hawking temperature, area-law entropy, information preserved. Phase IX (astrophysics and strong-field gravitation), target five, forward from the strong-field gravity result that resolves the singularity and proves the area law , the perpetually live vacuum , and the

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### IX-4
**Claim:** supernovae and the heavy elements -- core-collapse, thermonuclear, the r-process. Phase IX (astrophysics and strong-field gravitation), target four, forward from the inert iron core of stellar nucleosynthesis , the Chandrasekhar and TOV limits , the iron binding peak (V-4), and the neutr

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### IX-3
**Claim:** the degenerate endpoints -- Chandrasekhar and TOV limits from floored degeneracy pressure. Phase IX (astrophysics and strong-field gravitation), target three, forward from Fermi statistics and exclusion (I-5), the no-zero floor , stellar nucleosynthesis and the inert core , and the black

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### IX-2
**Claim:** stellar nucleosynthesis -- staged fusion up the binding curve to iron. Phase IX (astrophysics and strong-field gravitation), target two, forward from the binding-energy curve and its iron peak (V-4), the fusion thresholds (V-7), and stellar structure .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### IX-1
**Claim:** stellar structure and the main sequence -- gravity against fold-pressure, the mass-luminosity relation. Phase IX (astrophysics and strong-field gravitation), target one, forward from gravity , the thermal pressure as mean throw (I-1), and stellar fusion (V-7).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VIII-7
**Claim:** the fate of the universe -- eternal accelerating expansion with a perpetually live vacuum. Phase VIII (cosmology in full), target seven, the closing target of the phase, forward from the positive nonzero vacuum energy , the vacuum equation of state minus one , the perpetually-cycling vacuu

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VIII-6
**Claim:** inflation sharpened -- the e-folds and the red-tilted primordial spectrum. Phase VIII (cosmology in full), target six, forward from the inflation proven by the cosmological timeline , the fold expansion factor , the flatness , and the structure seeds .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VIII-5
**Claim:** structure formation -- gravitational fold-instability, the dark sector scaffolds. Phase VIII (cosmology in full), target five, forward from gravity , the dark sector , the early density field seeded by inflation , and recombination .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VIII-4
**Claim:** baryogenesis -- the surviving matter excess, the Sakharov conditions proven. Phase VIII (cosmology in full), target four, forward from the matter-antimatter asymmetry proven nonzero by the no-zero floor , the arrow of time from the two-to-one fold , the maximal CP violation (VI-6, )

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VIII-3
**Claim:** recombination and the cosmic microwave background -- acoustic peaks at harmonic positions, alternating heights. Phase VIII (cosmology in full), target three, forward from the thermal history , the hydrogen binding (Phase III), the plasma acoustics (Phase VII), and the baryon asymmetry .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VIII-2
**Claim:** big-bang nucleosynthesis -- the primordial helium fraction from neutron-proton freeze-out. Phase VIII (cosmology in full), target two, forward from the thermal history , the nuclear binding and the deuteron (Phase V), the weak sector , the freeze-out logic of the thermodynamics phase (P

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VIII-1
**Claim:** the proven thermal history -- temperature inversely with scale, the sequence of epochs. Phase VIII (cosmology in full), target one, forward from the fold expansion factor and the doubling map , the temperature as mean throw (I-1), confinement (V-1), nucleosynthesis and recombination thresholds

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VII-8
**Claim:** acoustics -- sound as the macroscopic phonon pressure wave, the wave equation from the lattice. Phase VII (plasma, optics, and continuous media), target eight, the closing target of the phase, forward from the phonon and the lattice wave modes (II-3), the lattice second-difference operator , an

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VII-7
**Claim:** blackbody radiation -- quantized modes freeze out, Wien and Stefan-Boltzmann proven. Phase VII (plasma, optics, and continuous media), target seven, forward from the canonical weighting and the thermodynamics phase (I-3), the Bose statistics (I-5), the oscillator tower , and the electromagneti

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VII-6
**Claim:** nonlinear optics -- second-harmonic generation and the Kerr effect from the fold self-coupling. Phase VII (plasma, optics, and continuous media), target six, forward from the linear refractive response , the gravitational self-source , and the strong self-coupling .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VII-5
**Claim:** the laser -- stimulated emission and the C7s lock of the radiation field above threshold. Phase VII (plasma, optics, and continuous media), target five, forward from the collective lock at criticality (C7s), the boson statistics that let photons share a mode (I-5), Bose-Einstein condensation (I-9),

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VII-4
**Claim:** geometric and wave optics -- Snell, reflection, interference, diffraction from wave-matching. Phase VII (plasma, optics, and continuous media), target four, forward from light as a wave (EM3), the refractive index , and the lattice wave structure.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VII-3
**Claim:** the refractive index -- bound-charge coupling slows the phase speed to c over n, the invariant speed unchanged. Phase VII (plasma, optics, and continuous media), target three, forward from light as an electromagnetic wave at the invariant speed (EM3), the bound atomic charges and their resonances (I

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VII-2
**Claim:** magnetohydrodynamics on the floored lattice -- finite flow, the Alfven wave. Phase VII (plasma, optics, and continuous media), target two, forward from the plasma state , the floored-lattice fluid result that bounds the vorticity , the electromagnetic field (EM), and the wave-speed struc

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VII-1
**Claim:** ionization and the plasma state -- the plasma frequency and Debye length as proven fold quantities. Phase VII (plasma, optics, and continuous media), target one, forward from the temperature as mean throw (I-1), the ionization binding of the shell structure (III-4), Coulomb's law (EM1), and the osci

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VI-7
**Claim:** neutrino oscillation -- the beat between mass states composing the flavour states. Phase VI (particle phenomenology), target seven, the closing target of the phase, forward from the neutrino mass-squared ladder , the large PMNS mixing angles as bare fold separations , and the Born str

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VI-6
**Claim:** CP violation and the proven phase -- intrinsic and maximal, the antipode fold-position. Phase VI (particle phenomenology), target six, forward from the CP-violating phase proven to the antipode , the closed unitarity triangle , and the arrow of time from the two-to-one fold .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VI-5
**Claim:** vacuum polarization -- the live vacuum screens a charge, the source of running and the Lamb shift. Phase VI (particle phenomenology), target five, forward from the perpetually-cycling live vacuum , the running of the couplings (VI-3), and the Lamb shift (III-3).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VI-4
**Claim:** renormalization without infinities -- the floored lattice makes every loop sum finite. Phase VI (particle phenomenology), target four, forward from the finite discrete fold structure of quantum gravity , the no-zero floor , the bounded-denominator finite-space mechanism , and the

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VI-3
**Claim:** the running of the couplings -- the holding form over depth, converging at high scale. Phase VI (particle phenomenology), target three, forward from the proven level-to-depth map and the single scale axis , the closed form of the coupling-convergence rate , and the running source s = m + two

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VI-2
**Claim:** decay widths and branching ratios -- the total fold-transition rate and its partition. Phase VI (particle phenomenology), target two, forward from the atomic transition rates (III-5), the nuclear decay law (V-6), and the Born rule .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### VI-1
**Claim:** cross-sections and scattering -- the Born probability of fold-deflection, Rutherford and Compton. Phase VI (particle phenomenology and quantum field structure), target one, forward from the measurement and Born rule , Coulomb's law and the inverse-square prove (EM1, ), and the conservation of

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### V-8
**Claim:** the deuteron and the lightest bound states -- spin-dependence and Pauli forbid the di-nucleon. Phase V (nuclear and hadronic structure), target eight, the closing target of the phase, forward from the residual nuclear prove (V-3), Fermi statistics and Pauli exclusion (I-5), and the handedness that i

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### V-7
**Claim:** fission and fusion -- energy release toward the iron peak, thresholds from the barriers. Phase V (nuclear and hadronic structure), target seven, forward from the binding-energy curve and its iron peak (V-4), the residual nuclear prove (V-3), Coulomb's law (EM1), and the temperature as mean throw (I-

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### V-6
**Claim:** radioactive decay -- the three modes as fold-transitions, the decay law a rational geometric. Phase V (nuclear and hadronic structure), target six, forward from the nuclear shells (V-5), the weak sector , the nuclear binding (V-4), and the atomic transitions (III-5).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### V-5
**Claim:** the nuclear shell and the magic numbers -- covering shells reordered by strong spin-orbit. Phase V (nuclear and hadronic structure), target five, forward from the covering tower , the atomic shell structure it parallels (III-4), the oscillator tower , the handedness that is the spin

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### V-4
**Claim:** nuclear binding and the valley of stability -- the binding curve peaking at iron. Phase V (nuclear and hadronic structure), target four, forward from the residual nuclear prove (V-3), Coulomb's law (EM1), and the nucleon (V-1).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### V-3
**Claim:** the nuclear prove as a residual -- the strong van der Waals, short range from a massive mediator. Phase V (nuclear and hadronic structure), target three, forward from the colour-neutral nucleon (V-1), the lightest mesons (V-2), confinement , the massive-mediator range , and the van der Wa

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### V-2
**Claim:** the hadron spectrum -- mesons and baryons the only colour-neutral combinations, linear Regge. Phase V (nuclear and hadronic structure), target two, forward from the three-colour structure , confinement , the nucleon (V-1), and the quark flavours of the matter sector.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### V-1
**Claim:** the nucleon as a bound three-quark fold -- mass dominated by binding, not quark mass. Phase V (nuclear and hadronic structure), target one, forward from proton stability and the colour-neutral group of three , confinement from flux in a tube , the strong self-coupling , and the q

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### IV-8
**Claim:** intermolecular proves -- the electromagnetic residual outside neutral molecules. Phase IV (chemistry), target eight, the closing target of the phase, forward from Coulomb's law (EM1), the electronegativity and bond polarity (IV-2), and the residual structure shared with the nuclear prove (Phase V).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### IV-7
**Claim:** stereochemistry and chirality -- the two-hand fold fibre at molecular scale. Phase IV (chemistry), target seven, forward from the chirality fibre that is the fold's handedness , the two-valued fold degree of freedom , and the molecular bond (III-7).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### IV-6
**Claim:** acids, bases, and equilibrium -- proton transfer and pH as a fold-ratio. Phase IV (chemistry), target six, forward from the reaction equilibrium (IV-3), the canonical weighting (I-3), and the transcendental-free count that replaces the logarithm (I-2).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### IV-5
**Claim:** catalysis -- an alternative fold-path with a lower barrier, enzyme specificity a shape-matched basin. Phase IV (chemistry), target five, forward from the reaction descent and its barrier (IV-3), the rate as the threshold-crossing fraction (IV-4), and the protein-folding basin .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### IV-4
**Claim:** reaction kinetics -- the rate as the fraction above the activation barrier, Arrhenius proven. Phase IV (chemistry), target four, forward from the reaction descent and its activation barrier (IV-3), the canonical weighting (I-3), and the temperature as mean throw (I-1).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### IV-3
**Claim:** reaction thermodynamics -- fold-descent between fixed points, enthalpy and activation proven. Phase IV (chemistry), target three, forward from the descent-to-a-fixed-point that folds proteins , the canonical weighting (I-3), the bond energies (III-7), and the energy landscape.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### IV-2
**Claim:** electronegativity and bond polarity -- binding depth and its difference. Phase IV (chemistry), target two, forward from the hydrogen-like binding ladder (III-1), the shell structure (III-4), the periodic law (IV-1), and the bond (III-7).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### IV-1
**Claim:** the periodic law -- recurrence of the covering pattern, valence as unpaired-handedness count. Phase IV (chemistry), target one, forward from the shell structure and its capacities (III-4), the covering tower , and the bond as a shared orbit of available electrons (III-7).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### III-8
**Claim:** molecular spectra -- the rotational J(J+1) ladder, the vibrational oscillator ladder, the isotope shift. Phase III (atomic and molecular physics), target eight, the closing target of the phase, forward from the covalent bond (III-7), the electronic levels (III-1), the oscillator tower , and th

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### III-7
**Claim:** the molecular bond -- a shared fold-orbit, the bond length a proven energy minimum. Phase III (atomic and molecular physics), target seven, forward from the collective lock at criticality (C7s), the atomic levels (III-1), the shell structure (III-4), and Coulomb's law (EM1).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### III-6
**Claim:** the Zeeman and Stark effects -- field splitting from handedness coupling and Coulomb displacement. Phase III (atomic physics), target six, forward from the handedness that carries the magnetic moment , Coulomb's law (EM1), the orientation sublevels (III-4), and the hydrogen ladder (III-1).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### III-5
**Claim:** selection rules, transition rates, and lifetimes -- one atomic fold-act transfers one unit. Phase III (atomic physics), target five, forward from the atomicity of the fold-act (C5s), the two-to-one structure , the handedness quantum , and the hydrogen ladder (III-1).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### III-4
**Claim:** the multi-electron atom and the shell structure -- capacities twice n-squared, the periodic recurrence. Phase III (atomic physics), target four, forward from the hydrogen ladder (III-1), Fermi exclusion (I-5), the handedness that doubles each orbital , and the covering tower .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### III-3
**Claim:** the Lamb shift -- the live cycling vacuum necessarily shifts the bound levels. Phase III (atomic physics), target three, forward from the perpetually-cycling vacuum , the hydrogen ladder (III-1), and the fine-structure constant .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### III-2
**Claim:** fine and hyperfine structure -- proven fractions of the gross ladder. Phase III (atomic physics), target two, forward from the hydrogen gross ladder (III-1), the fine-structure constant proven exactly , the fold-handedness that is the spin , and the electron-proton mass ratio of the mass s

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### III-1
**Claim:** the hydrogen spectrum -- the one-over-n-squared fold-ladder, the Rydberg energy from the proven fine-structure constant. Phase III (atomic and molecular physics), target one, forward from the fine-structure constant proven exactly , the electron absolute scale , Coulomb's law (EM1), and th

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### II-11
**Claim:** mechanical properties -- elasticity, plasticity, and fracture from the lattice-bond fold-energy. Phase II (condensed matter), target eleven, the closing target of the phase, forward from the lattice , the phonon restoring (II-3), the bond as a shared fold-orbit, and the lock threshold (m-1)/m.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### II-10
**Claim:** topological matter -- protected edge states from a fold-winding invariant. Phase II (condensed matter), target ten, forward from the quantum Hall winding count (II-9), the band gap (II-4), and the lattice .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### II-9
**Claim:** the quantum Hall effects -- Hall conductance as a proven rational count of fold-windings. Phase II (condensed matter), target nine, forward from the two-dimensional lattice , the magnetic handedness , and crucially the perpetually-cycling conserved odd-denominator modes .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### II-8
**Claim:** magnetism -- fold-handedness alignment, the Curie and Neel threshold, hysteresis. Phase II (condensed matter), target eight, forward from the chirality fibre that carries handedness , the collective lock at criticality (C7s, ), the lattice , and the temperature as mean throw (I

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### II-7
**Claim:** superfluidity -- the neutral-boson lock, frictionless flow below the critical velocity. Phase II (condensed matter), target seven, forward from the superconducting lock (II-6), the Bose condensation (I-9), the collective lock at criticality (C7s), and the wave speed bound (EM3, for the excitation sp

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### II-6
**Claim:** superconductivity -- the collective lock of paired carriers, zero resistance below the threshold. Phase II (condensed matter), target six, forward from the collective lock at criticality (C7s, ), the chirality fibre , the Bose pairing and condensation (I-5, I-9), and the temperature as mean t

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### II-5
**Claim:** semiconductor physics and the junction -- doping, the p-n junction, rectification. Phase II (condensed matter), target five, forward from the electronic bands and the small semiconductor gap (II-4), the level structure , and the Fermi occupation (I-5).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### II-4
**Claim:** electronic bands -- allowed bands and forbidden gaps, the conductor/insulator/semiconductor split. Phase II (condensed matter), target four, forward from the periodic fold-lattice , the crystallographic order (II-1), the level tower , and the Fermi occupation (I-5) against the thermal

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### II-3
**Claim:** phonons and the lattice spectrum -- gapless acoustic branch, Debye and Dulong-Petit heat capacity. Phase II (condensed matter), target three, forward from the framework lattice reproducing the monatomic chain , the three-dimensional cubic lattice operator , the proven three dimensions

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### II-2
**Claim:** quasicrystals -- forbidden five-fold order as a proven aperiodic fold-tiling. Phase II (condensed matter), target two, forward from the crystallographic restriction (II-1) and the fold as a doubling substitution.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### II-1
**Claim:** crystalline order and the crystallographic restriction -- only integer-trace rotations allowed. Phase II (condensed matter and the solid state), target one, forward from the three-dimensional cubic lattice operator , the proven three spatial dimensions , and the rational-magnitude constrai

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### I-10
**Claim:** Maxwell's demon and the information-entropy tie -- erasing a bit costs a proven minimum throw. Phase I (thermodynamics), target ten, the closing target of the phase, forward from the atomic one-bit act (C5s), the two-to-one irreversibility , the entropy-as-count (I-2), and the no-zero floor (

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### I-9
**Claim:** Bose-Einstein condensation -- the cold boson lock onto one shared ground orbit. Phase I (thermodynamics), target nine, forward from the Bose statistics (I-5), the temperature as mean throw (I-1), the canonical weighting (I-3), the criticality threshold (I-6, ), and the collective lock (C7s).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### I-8
**Claim:** irreversibility and the recurrence reconciliation -- two timescales, no contradiction. Phase I (thermodynamics), target eight, forward from the second law as the monotone configuration count (I-2), the periodicity of finite fold-systems , and the self's pattern-recurrence (C10s).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### I-7
**Claim:** fluctuation, dissipation, and noise -- tied by the shared periodic orbit. Phase I (thermodynamics), target seven, forward from the periodicity of the equilibrium orbit , the temperature as mean throw (I-1), and the canonical equilibrium (I-3).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### I-6
**Claim:** phase transitions and critical exponents -- at the threshold (m-1)/m, the exponents proven rational. Phase I (thermodynamics), target six, forward from the criticality threshold , the cross-domain identity of coupling and criticality , and the unity-at-criticality lock (C7s).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### I-5
**Claim:** quantum statistics -- Bose and Fermi from the two-to-one fold and the chirality fibre. Phase I (thermodynamics), target five, forward from the two-valued fold-level and the fermionic occupation rule , the chirality fibre's two preimages , and the canonical weighting (I-3).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### I-4
**Claim:** the four laws of thermodynamics, each proven from existing structure. Phase I (thermodynamics), target four, forward from the temperature as mean throw (I-1), the entropy-as-count and second law (I-2), the fold's conservation of magnitude, and the no-zero axiom .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### I-3
**Claim:** the canonical distribution -- the maximum-count equilibrium, a rational weighting, no exponential. Phase I (thermodynamics), target three, forward from the entropy-as-count and the second law (I-2), the temperature as mean throw (I-1), and the bounded-denominator finite state space .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### I-2
**Claim:** entropy as the fold-configuration count, the second law proven from the two-to-one fold. Phase I (thermodynamics), target two, forward from the arrow of time and the doubling Fock count , the antilog structure , and the temperature of target one (I-1).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### I-1
**Claim:** temperature -- the mean throw-rate of a folding population. Phase I (thermodynamics and statistical mechanics), target one, forward from the expansion factor and doubling-map structure , the criticality threshold , the arrow of time from the two-to-one fold , and the atomic moment (C5s

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**


## Section I. Self-observation and closure

*9 entries.*

### C1s
**Claim:** self-observation is closed. Observation is in the axiom set, and the fold is the act of observation: applied to a state, its result re-enters as part of the state.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### C5s
**Claim:** the discreteness of the observational moment. The fold is the unit act of observation and it is atomic : each fold yields one bit -- it either casts out a whole One or it does not, with no partial fold.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### C4s
**Claim:** integration of self-observers. The framework proves a holding threshold (m-1)/m at which coupled copies lock into one.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### C2s
**Claim:** the observation blind spot. The act of observation is two-to-one : a state and its antipode are observed identically, so self-observation cannot recover which of the two preimages it came from -- an intrinsic limit on self-knowledge proven by the 2-to-1 structure of the act [a self-model built

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### C3s
**Claim:** the self-observation fixed point. Unison (the One) is the one state unchanged by observation, fold(1)=1; the half-One observes to unison; self-coincidence below unison repels .

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### C9s
**Claim:** the felt self -- the unique fixed point of self-observation. Phase 5 of the self-observation sector, forward from C3s (the fixed point), C6s (the stream), and C1s (closure).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### C8s
**Claim:** the limit of self-knowledge -- the collapsed antipode, one lost bit per act. Phase 4 of the self-observation sector, forward from C2s (the blind spot) and C5s (the atomic act).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### C7s
**Claim:** the unity of experience -- one shared orbit, bound at the proven criticality threshold. Phase 3 of the self-observation sector, forward from C4s (the integration threshold) and (the proven cross-domain identity of the one ratio (m-1)/m).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### C6s
**Claim:** the stream of experience -- the chained orbit of moments, with an indivisible grain. Phase 2 of the self-observation sector, forward from C5s (the atomic moment) and (fold-depth is the time coordinate).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**


## Section J. Other

*21 entries.*

### C-3
**Claim:** the accessible artifact -- the unfolding rendered to a universal portable playable format. Chapter C (the universe simulation, the unfolding), target three, the closing target of the chapter, forward from the simulation kernel (C-1) and the unfolding sequence (C-2).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### C-2
**Claim:** the unfolding sequence -- the dependency-ordered playthrough, the movie as the derivation. Chapter C (the universe simulation, the unfolding), target two, forward from the simulation kernel (C-1) and the dependency structure of the whole corpus.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### C-1
**Claim:** the simulation kernel -- the framework running forward from the One, driven only by the fold. Chapter C (the universe simulation, the unfolding), target one, forward from the one-fold equation (A-1) and the master equation (A-3).

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### A-3
**Claim:** the master equation -- the single structure that, unfolded, carries the entire universe. Chapter A (the equations of everything in the fold framework), target three, the closing target of the chapter, assembled from the one-fold equation (A-1) and the sector equations (A-2) and verified to reproduce

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### A-2
**Claim:** the sector equations -- the proven equation for every sector, each tied to A-1. Chapter A (the equations of everything in the fold framework), target two, forward from the one-fold equation (A-1) and the proven sector results across the corpus.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### A-1
**Claim:** the one-fold equation -- the single closed generating law of the framework. Chapter A (the equations of everything in the fold framework), target one, forward from the One and the fold as the sole axiom and operation.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### C10s
**Claim:** cessation -- the lock releases, the anchor (unison) persists as the undestroyable One. Phase 6 of the self-observation sector, re-worked to its full proven truth, forward from C7s (unity is parts locked above the threshold), C9s (the felt self is the fixed point, unison), C3s (unison is the unique f

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### QA5
**Claim:** the full Dirac structure in three space and one time dimension. The four-generator first-order step -- three spatial momentum directions and the mass -- squares to the relativistic dispersion p1^2 + p2^2 + p3^2 + m^2: each generator applied twice gives its own squared term, and every distinct pair o

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### QA4
**Claim:** the relativistic two-component step squares to the relativistic dispersion (the Dirac structure). The two components are the two hands of the chirality fibre ; the first-order step advances each hand's phase by the momentum (the kinetic term, QA1) and couples each hand to the other by the mass

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### QA3
**Claim:** the stationary states of the quantum evolution are the proven spectrum. A stationary state turns at a single constant per-tick rate -- the analogue of a stationary state turning at constant rate E -- and the allowed stationary rates, built on the framework's proven half-One floor and un

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### QA2
**Claim:** quantum dynamics under a potential. A potential is a position-dependent rotation step -- the static local source of -- and the amplitude's phase advances each tick by the kinetic dispersion (QA1) together with the local potential, composed as positive rotation magnitudes on the One, never by a s

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### QA1
**Claim:** quantum dynamics -- the free-particle dispersion. A quantum amplitude is positive presence carrying a phase that advances by the framework's rotation (wave.rotate = cast_out of the phase plus a step), never by the imaginary unit or a complex exponential; the kinetic term is the lattice second-differ

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D6b
**Claim:** variance form of the uncertainty bound. The support bound s_t*s_f>=N=2^k is the count form; the variance form weights each support by its basis spacing.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D6
**Claim:** quantum structure beyond the oscillator -- complementarity/uncertainty as a count inequality. At depth k the space has N=2^k position-branches , uniformly spaced ; the fold is the bit shift , so the fold bits are the Walsh/Rademacher generators and the conjugate frequency basis is the Wa

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D4
**Claim:** spacetime / causal structure: the Minkowski causal structure built in positive magnitudes. The Lorentzian signature (the minus sign between time and space) is carried by the audited take (a positive difference), not a signed metric: the temporal magnitude c*dt and spatial magnitude dx are combined b

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D3
**Claim:** interaction / mode coupling: the framework's native nonlinearity is the fold (the doubling map mixes modes). Its operations prove the products of second-order three-wave mixing -- second harmonic 2f (fold), sum frequency f1+f2 (cast_out of the phase sum), difference frequency |f1-f2| (take; = the be

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D2
**Claim:** propagation law / wave equation: the 1D wave equation general solution (dAlembert) is two waves moving in opposite directions at speed c [LibreTexts; Wikipedia dAlembert]. The framework reproduces it in positive presence: a disturbance splits into a right-moving and a left-moving positive packet, ea

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D1d
**Claim:** three-dimensional (cubic) lattice operator. The plane operator extends to a cube.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D1c
**Claim:** two-dimensional lattice operator. Extends the 1D lattice and 1D curvature to a plane.

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D1
**Claim:** field / coupled lattice: the framework lattice (positive presence redistribution, diffusive pull , built in positive magnitudes -- no negatives, no signed Laplacian) reproduces the 1D monatomic chain: presence conserved, a disturbance front advances one site per tick (finite propagation speed), N

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**

### D1b
**Claim:** algebraic-magnitude engine: an incommensurable magnitude (the diagonal of the unit square; a lattice mode frequency) is represented as the positive point where two positive-coefficient polynomials balance, P(x)=Q(x), certified by the order-swap of the two positive sides across a rational interval an

**Derivation requirement:** compute the stated quantity forward from the One and the fold; verify by comparing the computed value to an independently-derived structural value (a different route through the fold), never to a typed copy of the target or an always-true identity. If the claim needs a measured number, a square root, an integral, or an absolute scale, it is an EXTERNAL READ — compute the dimensionless part forward and mark the rest. **Check must fail under mutation.**
